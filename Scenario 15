<?php
$page = $_GET['page'];
$limit = 5;

// WRONG: no validation leads to the possibility of the user trying to input a super large page number and the MySQL might crash
// $offset = $page * $limit;
// $sql = "SELECT * FROM students LIMIT $offset, $limit";

// CORRECT: validate and restrict the page number
$page = (int) $page; // siguraduhin number
if($page < 0) {
    $page = 0; // minimum page 0
} elseif($page > 1000) { // maximum page limit para di ma-crash ang server
    $page = 1000;
}

$offset = $page * $limit;
$sql = "SELECT * FROM students LIMIT $offset, $limit";
$res = mysqli_query($conn, $sql);

/*
included validation for the page number. 
used (int) cast at min/max limit, so that it would limit the inputs of numbers.
*/
?>
