<?php
// WRONG: used $_POST  even though the form method was GET
// $email = $_POST['email'];

 // CORRECT: use $_GET since the form method is GET
$email = $_GET['email'];

//
replaced  $_POST to $_GET since the form used the method = "GET". 
?>
